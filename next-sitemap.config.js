/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://www.euclidlearn.com', // Replace with your website's URL
    generateRobotsTxt: true, // Automatically generate a robots.txt file
    sitemapSize: 5000, // Optional, split sitemaps if you have too many URLs
  };
  
  module.exports = config;
  